var multiply = function(num1, num2) {
	let result = '0';

	if(num1 == '0') return num2;
	if(num2 == '0') return num1;
	// Long hand Multiplication
	// Traverse shorter string, multiply each char by all the other chars in the longer string, sum the  results together

	return result;
};

module.exports = multiply;
